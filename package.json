{
  "name": "share-talk-heal",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "frontend": "cd frontend && npm install && npm run dev",
    "backend": "cd backend && npm install && npm run dev",
    "install:all": "cd frontend && npm install && cd ../backend && npm install"
  }
}
